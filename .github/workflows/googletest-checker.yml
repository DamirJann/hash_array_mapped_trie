name: googletest-project
on:
  push:
    paths-ignore:
      - 'resources/**'
      - 'README.md'

jobs:
  test-project:
    name: Test Project
    runs-on: ubuntu-latest
    steps:
      - name: Test project with googletest.
        uses: damirjann/hash_array_mapped_trie@not_concurrent_hamt